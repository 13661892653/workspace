package com.easyrong.data.days03;

public class IfElseDemo {
    public static void main(String args[]) {
        //局部变量
        //类变量（静态变量）
        //成员变量（非静态变量）
        /*
        if..else语句格式
        if(条件){
            if执行体;
        }else{
            else执行体;
        }
        */
    }
}
